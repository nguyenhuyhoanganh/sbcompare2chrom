"""What an agent needs to answer a question about a report.

The pieces here are deliberately not an agent. They are the parts an agent
runs -- a command, a query over the report -- bounded so that a wrong one
costs a bounded amount rather than the session.
"""
